import React from 'react';

interface Props {
    img: string;
    text: string;
    score: string;
}

const ScoreLine: React.FC<Props> = (props: Props) => {
    return <></>;
};

export default ScoreLine;
